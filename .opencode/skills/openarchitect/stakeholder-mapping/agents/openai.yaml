interface:
  display_name: "Stakeholder Mapping"
  short_description: "Map stakeholders with orchestrated specialist prompts."
  default_prompt: "Run stakeholder mapping using agents/sub-agents.yaml and specialist prompts."
dependencies:
  tools:
    - type: "mcp"
      value: "openarchitect"
      description: "OpenArchitect context platform MCP server"
      transport: "http"
      url: "http://localhost:8101/mcp"

