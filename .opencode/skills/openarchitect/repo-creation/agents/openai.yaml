interface:
  display_name: "Repo Creation"
  short_description: "Create governed repository outputs with orchestrated specialist prompts."
  default_prompt: "Create or verify repositories using agents/sub-agents.yaml and specialist prompts."
dependencies:
  tools:
    - type: "mcp"
      value: "openarchitect"
      description: "OpenArchitect context platform MCP server"
      transport: "http"
      url: "http://localhost:8101/mcp"

